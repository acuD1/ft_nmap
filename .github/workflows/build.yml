name: C CI

on: pull_request

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
    - name: env
      run: pip3 install -r requierements.txt
    - name: test
      run: make test
